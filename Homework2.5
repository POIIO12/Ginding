#include <stdio.h>
#include <string>
using namespace std;
int main()
{
    int num;
    cout << "What number do you want the table of?";
    cin >> num;
    for (int yikes = 0; yikes <= 12; yikes = yikes + 1){
        cout << yikes * num;
    }
    
    
    char c;
    string name;
    cout << "what is ur name please enter all lower case" << endl;
    cin >> name;
    for (int i = 0; i < name.length(); i++){
        char capital = name[i] - 32;
        cout << capital << endl;
    }

    //first i need to make a loop that divides the number by all of its previous numbers
    int num = 1;
    int limit;
    cout << "What prime number in this range do you want to find?";
    cin >> limit;
    while (num <= limit){
        //Now only print a number if it is an interger. But how do I tell him that?
        if (/*quotient is interger*/){
            cout << limit / num << endl;
        }
        num = num + 1;
    }

    return 0;
}
